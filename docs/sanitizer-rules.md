# Sanitizer drop rules

| Reason | Where in code | Exact check / regex | Affects which columns | Notes |
| --- | --- | --- | --- | --- |
| Row failed to parse | `sanitizeObservationCues` skips any entry that is not an array when walking the CSV data rows.【F:scripts/sanitizeObservationCues.mjs†L46-L50】 | `if (!Array.isArray(row)) { dropped += 1; return; }` | Entire row | Guards against malformed CSV rows before any cue-specific checks run. |
| Example sanitizes to empty | `sanitizeObservationCues` drops rows whose example collapses to an empty string after `sanitizeObservationText`.【F:scripts/sanitizeObservationCues.mjs†L52-L57】 | `if (!sanitizedExample) { dropped += 1; return; }` | Example column (propagates to cue removal) | `sanitizeObservationText` trims input, segments sentences, strips flagged tokens, and bails out when lint flags evaluation language, catalog hits, or unsalvageable groups.【F:lib/observationSanitize.js†L3-L93】【F:lib/observationSanitize.js†L95-L158】 |
| Non-observational flags (details of prior row) | `sanitizeObservationText` returns `''` when lint finds blocking issues: evaluation words, phrases, or agentive markers; faux feelings/feelings/needs hits; or flagged groups outside the salvageable list. Tokens from those groups are stripped outside quotes; if nothing usable remains, the example is considered empty and the row is dropped by the caller.【F:lib/observationSanitize.js†L35-L93】【F:lib/observationSanitize.js†L95-L158】【F:lib/nvcLint.js†L1-L205】【F:lib/nvcLint.js†L224-L343】 | Example column | Covers faux-feeling words, character labels, moralizing language, global language, mind-reading, blame, etc., as enumerated in `FLAG_GROUP_DEFS`, plus any catalog matches. |
| Example duplicates a prior sanitized example | Global `seenExamples` set drops repeated sanitized examples (case-insensitive).【F:scripts/sanitizeObservationCues.mjs†L70-L77】 | `if (seenExamples.has(exampleKey)) { dropped += 1; duplicateDrops += 1; return; }` | Example column (global scope) | Ensures every sanitized example is unique across the entire sheet, not just per cue. |
| Faux feeling text detected anywhere in the row | `containsFauxFeeling` builds `\b(?:token)(?:[-\s]+token)...\b` matchers from faux-feeling slugs and rejects any row whose cue, patterns, feelings, needs, or example cell matches one.【F:scripts/sanitizeObservationCues.mjs†L60-L67】【F:scripts/sanitizeObservationCues.mjs†L229-L266】 | `return matchers.some(regex => regex.test(cell));` with pattern `\btoken(?:[-\s]+token)*\b` | All columns (cue, patterns, feelings, needs, example) | Faux-feelings are sourced from the catalog; slug tokens allow hyphen or whitespace in the CSV cells. |
| Missing required header | Sanitizer throws before processing if it cannot find an `example` column.【F:scripts/sanitizeObservationCues.mjs†L37-L42】 | Throws `Expected an "example" column` | File-level | Fails the run early instead of producing partial output. |
