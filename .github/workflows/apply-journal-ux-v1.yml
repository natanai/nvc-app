name: Apply Journal UX V1 Once

on:
  pull_request:
    branches: [ux-audit]
    types: [opened, synchronize]
    paths:
      - 'scripts/apply-journal-ux-v1.trigger'

permissions:
  contents: write

jobs:
  apply-journal-ux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout ux-audit base
        uses: actions/checkout@v4
        with:
          ref: ux-audit
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Integrate, regenerate, and smoke-test Journal
        run: node scripts/apply-journal-ux-v1.mjs

      - name: Confirm only expected durable files changed
        run: |
          git status --short
          unexpected="$(git status --porcelain | awk '{print $2}' | grep -Ev '^(scripts/build-pages\.mjs|inventory/journal/index\.html)$' || true)"
          if [[ -n "$unexpected" ]]; then
            echo "Unexpected generated changes detected:"
            echo "$unexpected"
            exit 1
          fi

      - name: Remove one-shot integration machinery
        run: git rm .github/workflows/apply-journal-ux-v1.yml scripts/apply-journal-ux-v1.mjs scripts/apply-journal-ux-v1.trigger

      - name: Commit durable Journal UX changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scripts/build-pages.mjs inventory/journal/index.html
          git commit -m "Integrate Journal UX into generated page"
          git push origin HEAD:ux-audit
