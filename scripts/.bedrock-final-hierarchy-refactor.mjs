import { readFileSync, writeFileSync } from 'node:fs';

const read = (path) => readFileSync(path, 'utf8');
const write = (path, value) => writeFileSync(path, value, 'utf8');

function replaceOnce(source, search, replacement, label) {
  const first = source.indexOf(search);
  if (first < 0) throw new Error(`Missing ${label}`);
  if (source.indexOf(search, first + search.length) >= 0) throw new Error(`Non-unique ${label}`);
  return source.slice(0, first) + replacement + source.slice(first + search.length);
}

function replaceRange(source, startMarker, endMarker, replacement, label) {
  const start = source.indexOf(startMarker);
  if (start < 0) throw new Error(`Missing ${label} start`);
  const end = source.indexOf(endMarker, start + startMarker.length);
  if (end < 0) throw new Error(`Missing ${label} end`);
  return source.slice(0, start) + replacement + source.slice(end + endMarker.length);
}

function replaceCssRule(source, selector, body, label = selector) {
  const start = source.indexOf(selector);
  if (start < 0) throw new Error(`Missing CSS rule ${label}`);
  const brace = source.indexOf('{', start + selector.length);
  if (brace < 0) throw new Error(`Missing opening brace for ${label}`);
  let depth = 0;
  let end = -1;
  for (let index = brace; index < source.length; index += 1) {
    if (source[index] === '{') depth += 1;
    else if (source[index] === '}') {
      depth -= 1;
      if (depth === 0) {
        end = index + 1;
        break;
      }
    }
  }
  if (end < 0) throw new Error(`Missing closing brace for ${label}`);
  return source.slice(0, start) + `${selector} {\n${body}\n}` + source.slice(end);
}

// 1. Page compiler: Journal becomes history-first and secondary tools become native disclosures.
//    Need strategy controls share the section header instead of consuming a separate toolbar row.
{
  const path = 'scripts/build-pages.mjs';
  let source = read(path);

  const journalStyleStart = "  const journalPageStyles = `    <style>\n      /* Journal UX first pass v1 — inline pre-paint overrides */";
  const journalStyleEnd = "    </style>`;";
  const journalStyles = `  const journalPageStyles = \`    <style>\n      /* Journal final shell: history first, compact controls, secondary disclosures. */\n      main[data-page-id='inventory-journal'] {\n        gap: clamp(0.65rem, 1.8vw, 0.95rem);\n      }\n\n      main[data-page-id='inventory-journal'] .journal-page-header {\n        padding: 0.2rem 0.08rem 0.35rem;\n        border: 0;\n        border-radius: 0;\n        background: transparent;\n        box-shadow: none;\n        gap: 0;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-page-title-row {\n        display: flex;\n        align-items: center;\n        justify-content: space-between;\n        gap: 0.65rem;\n        min-width: 0;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-page-header .page-title {\n        display: block;\n        position: static;\n        width: auto;\n        height: auto;\n        margin: 0;\n        overflow: visible;\n        clip: auto;\n        white-space: normal;\n        font-size: clamp(1.45rem, 5.6vw, 2rem);\n        line-height: 1.05;\n        text-transform: none;\n        letter-spacing: -0.015em;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-page-title-row__actions {\n        width: auto;\n        display: flex;\n        flex: 0 0 auto;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-fullscreen-button--compact {\n        min-width: 0;\n        min-height: 44px;\n        padding: 0.42rem 0.7rem;\n        gap: 0.42rem;\n        border: 1.5px solid color-mix(in srgb, var(--outline) 58%, transparent);\n        border-radius: var(--radius-pill);\n        background: color-mix(in srgb, var(--rose) 56%, #ffffff 44%);\n        color: var(--ink);\n        box-shadow: none;\n        font-weight: 750;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-fullscreen-button--compact:hover,\n      main[data-page-id='inventory-journal'] .journal-fullscreen-button--compact:focus-visible {\n        transform: none;\n        background: color-mix(in srgb, var(--rose) 68%, #ffffff 32%);\n        box-shadow: none;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-fullscreen-button--compact .journal-fullscreen-button__icon {\n        width: 1.45rem;\n        height: 1.45rem;\n        min-width: 1.45rem;\n        min-height: 1.45rem;\n        border-width: 1.5px;\n        box-shadow: none;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-fullscreen-button--compact .journal-fullscreen-button__title {\n        font-size: 0.82rem;\n        letter-spacing: 0;\n        text-transform: none;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-page {\n        display: grid;\n        gap: clamp(0.65rem, 1.8vw, 0.95rem);\n      }\n\n      main[data-page-id='inventory-journal'] .journal-history-section {\n        padding: clamp(0.7rem, 2vw, 0.9rem);\n        border: 1.5px solid color-mix(in srgb, var(--outline) 22%, transparent);\n        border-radius: var(--radius-2xl);\n        background: color-mix(in srgb, #ffffff 97%, var(--lavender) 3%);\n        box-shadow: none;\n        display: grid;\n        gap: 0.62rem;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-history-section__header {\n        display: flex;\n        align-items: baseline;\n        justify-content: space-between;\n        gap: 0.55rem;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-history-section__header .section-title {\n        font-size: 1.02rem;\n        line-height: 1.15;\n        letter-spacing: 0;\n        text-transform: none;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-history-controls {\n        display: grid;\n        gap: 0.48rem;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-history-controls__search input {\n        min-height: 44px;\n        border: 1px solid color-mix(in srgb, var(--outline) 18%, transparent);\n        border-radius: var(--radius-lg);\n        background: #ffffff;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-history-controls__filters {\n        display: flex;\n        gap: 0.34rem;\n        overflow-x: auto;\n        overscroll-behavior-inline: contain;\n        scrollbar-width: none;\n        padding-bottom: 0.08rem;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-history-controls__filters::-webkit-scrollbar {\n        display: none;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-history-control {\n        flex: 0 0 auto;\n        display: block;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-history-control > span {\n        position: absolute;\n        width: 1px;\n        height: 1px;\n        padding: 0;\n        margin: -1px;\n        overflow: hidden;\n        clip: rect(0, 0, 0, 0);\n        white-space: nowrap;\n        border: 0;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-history-control select,\n      main[data-page-id='inventory-journal'] .journal-history-controls__clear {\n        width: auto;\n        min-width: 0;\n        min-height: 40px;\n        padding: 0.38rem 1.55rem 0.38rem 0.62rem;\n        border: 1px solid color-mix(in srgb, var(--outline) 18%, transparent);\n        border-radius: var(--radius-pill);\n        background-color: color-mix(in srgb, #ffffff 92%, var(--lavender) 8%);\n        box-shadow: none;\n        color: var(--ink);\n        font: inherit;\n        font-size: 0.76rem;\n        font-weight: 650;\n        white-space: nowrap;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-history-controls__clear {\n        padding-inline: 0.7rem;\n        color: var(--ink-soft);\n      }\n\n      main[data-page-id='inventory-journal'] .journal-history--cards {\n        display: grid;\n        gap: 0;\n        overflow: hidden;\n        border: 1px solid color-mix(in srgb, var(--outline) 14%, transparent);\n        border-radius: var(--radius-xl);\n        background: #ffffff;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-entry {\n        margin: 0;\n        padding: 0.72rem 0.78rem;\n        gap: 0.42rem;\n        border: 0;\n        border-bottom: 1px solid color-mix(in srgb, var(--outline) 10%, transparent);\n        border-radius: 0;\n        background: #ffffff;\n        box-shadow: none;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-entry:last-child {\n        border-bottom: 0;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-entry__emotion {\n        margin: 0;\n        font-size: 0.96rem;\n        line-height: 1.25;\n        font-weight: 760;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-entry__meta {\n        font-size: 0.72rem;\n        line-height: 1.25;\n        color: var(--ink-soft);\n      }\n\n      main[data-page-id='inventory-journal'] .journal-entry__notes {\n        margin: 0;\n        font-size: 0.86rem;\n        line-height: 1.38;\n        color: color-mix(in srgb, var(--ink) 84%, var(--ink-soft) 16%);\n      }\n\n      main[data-page-id='inventory-journal'] .journal-entry__facets {\n        gap: 0.28rem;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-value-token {\n        min-height: 28px;\n        padding: 0.16rem 0.45rem;\n        border-width: 1px;\n        box-shadow: none;\n        font-size: 0.72rem;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-entry__actions {\n        margin-top: 0.08rem;\n        display: flex;\n        justify-content: flex-end;\n        gap: 0.34rem;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-entry__edit,\n      main[data-page-id='inventory-journal'] .journal-entry__delete {\n        min-width: 0;\n        min-height: 36px;\n        padding: 0.3rem 0.55rem;\n        border: 0;\n        border-radius: var(--radius-pill);\n        background: transparent;\n        box-shadow: none;\n        color: var(--ink-soft);\n        font: inherit;\n        font-size: 0.74rem;\n        font-weight: 700;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-entry__edit:hover,\n      main[data-page-id='inventory-journal'] .journal-entry__edit:focus-visible,\n      main[data-page-id='inventory-journal'] .journal-entry__delete:hover,\n      main[data-page-id='inventory-journal'] .journal-entry__delete:focus-visible {\n        transform: none;\n        background: color-mix(in srgb, var(--lavender) 38%, #ffffff 62%);\n        box-shadow: none;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-overview-grid {\n        display: grid;\n        grid-template-columns: minmax(0, 1fr);\n        gap: 0.48rem;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-utility-disclosure {\n        border: 1px solid color-mix(in srgb, var(--outline) 16%, transparent);\n        border-radius: var(--radius-xl);\n        background: color-mix(in srgb, #ffffff 94%, var(--lavender) 6%);\n        box-shadow: none;\n        overflow: hidden;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-utility-disclosure__summary {\n        min-height: 48px;\n        padding: 0.62rem 0.72rem;\n        display: flex;\n        align-items: center;\n        justify-content: space-between;\n        gap: 0.65rem;\n        cursor: pointer;\n        list-style: none;\n        font-size: 0.84rem;\n        font-weight: 760;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-utility-disclosure__summary::-webkit-details-marker {\n        display: none;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-utility-disclosure__chevron {\n        color: var(--ink-soft);\n        transition: transform 0.16s ease;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-utility-disclosure[open] .journal-utility-disclosure__chevron {\n        transform: rotate(90deg);\n      }\n\n      main[data-page-id='inventory-journal'] .journal-utility-disclosure__body {\n        display: grid;\n        gap: 0.55rem;\n        padding: 0 0.72rem 0.72rem;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-actions__hint {\n        margin: 0;\n        font-size: 0.76rem;\n        line-height: 1.35;\n        color: var(--ink-soft);\n      }\n\n      main[data-page-id='inventory-journal'] .journal-actions__buttons {\n        display: grid;\n        grid-template-columns: repeat(2, minmax(0, 1fr));\n        gap: 0.42rem;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-actions__buttons .inventory-button {\n        width: 100%;\n        min-height: 42px;\n        padding: 0.4rem 0.58rem;\n        border-width: 1.5px;\n        border-radius: var(--radius-lg);\n        box-shadow: none;\n        font-size: 0.74rem;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-summary {\n        padding-top: 0.05rem;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-inline-fallback {\n        margin-top: 0;\n        border: 0;\n        background: transparent;\n        box-shadow: none;\n      }\n\n      main[data-page-id='inventory-journal'] .journal-inline-fallback__summary {\n        min-height: 40px;\n        padding: 0.35rem 0.15rem;\n        font-size: 0.72rem;\n        color: var(--ink-soft);\n      }\n\n      @media (min-width: 760px) {\n        main[data-page-id='inventory-journal'] .journal-overview-grid {\n          grid-template-columns: repeat(2, minmax(0, 1fr));\n        }\n\n        main[data-page-id='inventory-journal'] .journal-history-controls__filters {\n          overflow-x: visible;\n          flex-wrap: wrap;\n        }\n      }\n\n      @media (max-width: 420px) {\n        main[data-page-id='inventory-journal'] .journal-page-header .page-title {\n          font-size: 1.35rem;\n        }\n\n        main[data-page-id='inventory-journal'] .journal-fullscreen-button--compact {\n          padding-inline: 0.58rem;\n        }\n\n        main[data-page-id='inventory-journal'] .journal-actions__buttons {\n          grid-template-columns: minmax(0, 1fr);\n        }\n      }\n    </style>\`;`;
  source = replaceRange(source, journalStyleStart, journalStyleEnd, journalStyles, 'Journal page styles');

  const journalMainStart = "  const main = `\n      <header class=\"page-header journal-page-header\">";
  const journalMainEnd = "      <script type=\"application/json\" id=\"journal-needs-data\">${escapeHtml(needsJson)}</script>\n    `;";
  const journalMain = `  const main = \`\n      <header class="page-header journal-page-header">\n        <div class="journal-page-title-row">\n          <h1 class="page-title">Journal</h1>\n          <div class="journal-page-title-row__actions">\n            <button\n              type="button"\n              class="journal-fullscreen-button journal-fullscreen-button--compact"\n              data-support-journal-open\n              aria-expanded="false"\n              aria-controls="global-support-journal-layer"\n            >\n              <span class="journal-fullscreen-button__icon" aria-hidden="true"></span>\n              <span class="journal-fullscreen-button__title">New entry</span>\n            </button>\n          </div>\n        </div>\n      </header>\n      <section class="journal-page" data-inventory-section="journal">\n        <section class="journal-history-section journal-panel journal-panel--history" aria-labelledby="journal-history-heading">\n          <div class="journal-history-section__header">\n            <h2 id="journal-history-heading" class="section-title">History</h2>\n          </div>\n          <form class="journal-history-controls" data-journal-filters>\n            <div class="journal-history-controls__search">\n              <label class="visually-hidden" for="journal-filter-search">Search journal</label>\n              <input id="journal-filter-search" name="search" type="search" placeholder="Search journal" autocomplete="off" />\n            </div>\n            <div class="journal-history-controls__filters" aria-label="Filter journal history">\n              <label class="journal-history-control" for="journal-filter-emotion"><span>Feeling</span><select id="journal-filter-emotion" name="emotion" aria-label="Feeling"><option value="">Feeling</option></select></label>\n              <label class="journal-history-control" for="journal-filter-need"><span>Need</span><select id="journal-filter-need" name="need" aria-label="Need"><option value="">Need</option></select></label>\n              <label class="journal-history-control" for="journal-filter-tag"><span>Tag</span><select id="journal-filter-tag" name="tag" aria-label="Tag"><option value="">Tag</option></select></label>\n              <label class="journal-history-control" for="journal-filter-range"><span>Date</span><select id="journal-filter-range" name="range" aria-label="Date"><option value="all">Any time</option><option value="7">7 days</option><option value="30">30 days</option><option value="90">90 days</option></select></label>\n              <label class="journal-history-control" for="journal-filter-sort"><span>Sort</span><select id="journal-filter-sort" name="sort" aria-label="Sort"><option value="newest">Newest</option><option value="oldest">Oldest</option><option value="intensity-high">Highest intensity</option><option value="intensity-low">Lowest intensity</option></select></label>\n              <button type="button" class="journal-history-controls__clear" data-journal-filters-reset>Clear</button>\n            </div>\n          </form>\n          <p class="journal-empty" data-journal-empty hidden>Save an entry to see it here.</p>\n          <div class="journal-history journal-history--cards" data-journal-history></div>\n        </section>\n\n        <div class="journal-overview-grid" aria-label="Journal tools">\n          <details class="journal-summary-section journal-utility-disclosure">\n            <summary class="journal-utility-disclosure__summary">\n              <span>Patterns</span>\n              <span class="journal-utility-disclosure__chevron" aria-hidden="true">›</span>\n            </summary>\n            <div class="journal-utility-disclosure__body">\n              <div class="journal-summary" data-journal-summary></div>\n            </div>\n          </details>\n\n          <details class="journal-actions journal-utility-disclosure">\n            <summary class="journal-utility-disclosure__summary">\n              <span>Backup &amp; restore</span>\n              <span class="journal-utility-disclosure__chevron" aria-hidden="true">›</span>\n            </summary>\n            <div class="journal-utility-disclosure__body">\n              <p class="journal-actions__hint">Export or import your journal, inventory, and customizer settings.</p>\n              <div class="journal-actions__buttons">\n                <button type="button" id="journal-export" class="inventory-button">Export</button>\n                <button type="button" id="journal-import-trigger" class="inventory-button inventory-button--ghost">Import</button>\n                <input type="file" id="journal-import" accept="application/json,.json,text/csv,.csv" hidden />\n              </div>\n              <p class="journal-message" data-journal-message hidden aria-live="polite"></p>\n            </div>\n          </details>\n        </div>\n\n        <details class="journal-inline-fallback" data-journal-inline-fallback>\n          <summary class="journal-inline-fallback__summary">\n            <span class="journal-inline-fallback__summary-text">Trouble opening the journal?</span>\n          </summary>\n          <div class="journal-inline-fallback__body">\n            <p class="journal-inline-fallback__note">Use this inline form only if the full-screen editor will not open.</p>\n            <div class="journal-inline-container journal-panel journal-panel--form-shell" data-journal-inline-container>\n              <section class="journal-form-section" aria-labelledby="journal-form-heading">\n                <div class="journal-form-section__header">\n                  <h2 id="journal-form-heading" class="section-title">New entry</h2>\n                </div>\n                <div class="journal-module" data-journal-module data-journal-variant="inventory" data-journal-id-prefix="journal">\n                  <noscript><p class="journal-status">Enable JavaScript to use the journal form.</p></noscript>\n                </div>\n              </section>\n            </div>\n          </div>\n        </details>\n      </section>\n      <script type="application/json" id="journal-needs-data">${'${escapeHtml(needsJson)}'}</script>\n    \`;`;
  source = replaceRange(source, journalMainStart, journalMainEnd, journalMain, 'Journal page markup');

  const oldStrategyHeader = `          <h2 id="strategy-heading" class="section-title">Strategies</h2>\n          <div class="strategy-deck-header">\n            <button\n              type="button"\n              class="strategy-deck__shuffle"\n              data-strategy-shuffle\n            >\n              Shuffle cards\n            </button>\n            <button\n              type="button"\n              class="strategy-deck__toggle"\n              data-strategy-toggle\n              aria-pressed="false"\n            >\n              View all\n            </button>\n          </div>`;
  const newStrategyHeader = `          <div class="strategy-section__header">\n            <h2 id="strategy-heading" class="section-title">Strategies</h2>\n            <div class="strategy-deck-header" aria-label="Strategy browsing controls">\n              <button type="button" class="strategy-deck__shuffle" data-strategy-shuffle aria-label="Shuffle strategy cards">Shuffle</button>\n              <button type="button" class="strategy-deck__toggle" data-strategy-toggle aria-pressed="false">View all</button>\n            </div>\n          </div>`;
  source = replaceOnce(source, oldStrategyHeader, newStrategyHeader, 'Need strategy section header');

  write(path, source);
}

// 2. Runtime: the browser-native Patterns disclosure owns its own expanded state.
{
  const path = 'scripts/inventory.js';
  let source = read(path);
  source = replaceOnce(source, '  journalSummaryToggle: null,\n', '', 'Journal summary toggle state');
  source = replaceOnce(source, '  journalSummaryCollapsed: false,\n', '', 'Journal summary collapsed state');
  source = replaceOnce(source, "    state.journalSummaryToggle = panel.querySelector('[data-journal-summary-toggle]');\n", '', 'Journal summary toggle query');
  source = replaceOnce(
    source,
    `    if (state.journalSummaryToggle) {\n      state.journalSummaryToggle.addEventListener('click', () => {\n        state.journalSummaryCollapsed = !state.journalSummaryCollapsed;\n        updateJournalSummaryVisibility();\n      });\n    }\n\n`,
    '',
    'Journal summary toggle listener',
  );
  source = source.replaceAll('    updateJournalSummaryVisibility();\n', '');
  source = source.replaceAll('  updateJournalSummaryVisibility();\n', '');
  const visibilityStart = 'function updateJournalSummaryVisibility() {';
  const visibilityEnd = 'function updateJournalIntensityDisplay(value) {';
  source = replaceRange(source, visibilityStart, visibilityEnd, 'function updateJournalIntensityDisplay(value) {', 'Journal summary visibility function');
  write(path, source);
}

// 3. Canonical shared styles: strategy browsing becomes a compact header utility; personal strategy editor becomes a lightweight grouped form.
{
  const path = 'styles.css';
  let source = read(path);

  source = replaceCssRule(source, '.strategy-section', `  display: grid;\n  gap: 0.58rem;`, 'strategy section');
  source = replaceCssRule(source, '.strategy-deck-header', `  display: flex;\n  align-items: center;\n  justify-content: flex-end;\n  gap: 0.3rem;\n  flex: 0 0 auto;`, 'strategy deck header');
  const headerInsertion = `.strategy-section__header {\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  gap: 0.65rem;\n  min-width: 0;\n}\n\n`;
  const deckHeaderIndex = source.indexOf('.strategy-deck-header');
  if (deckHeaderIndex < 0) throw new Error('Missing strategy deck header after replacement');
  if (!source.includes('.strategy-section__header {')) source = source.slice(0, deckHeaderIndex) + headerInsertion + source.slice(deckHeaderIndex);

  source = replaceCssRule(source, '.strategy-deck__shuffle,\n.strategy-deck__toggle', `  min-width: 0;\n  min-height: 44px;\n  padding: 0.38rem 0.68rem;\n  border: 1.5px solid color-mix(in srgb, var(--outline) 45%, transparent);\n  border-radius: var(--radius-pill);\n  background: color-mix(in srgb, var(--rose) 44%, #ffffff 56%);\n  color: var(--ink);\n  box-shadow: none;\n  font-family: var(--font-body);\n  font-size: 0.76rem;\n  font-weight: 720;\n  line-height: 1;\n  text-transform: none;\n  letter-spacing: 0;\n  cursor: pointer;\n  transition: background 0.14s ease, border-color 0.14s ease;`, 'strategy deck utility buttons');
  source = replaceCssRule(source, '.strategy-deck__shuffle:hover,\n.strategy-deck__shuffle:focus-visible,\n.strategy-deck__toggle:hover,\n.strategy-deck__toggle:focus-visible', `  transform: none;\n  background: color-mix(in srgb, var(--rose) 58%, #ffffff 42%);\n  border-color: color-mix(in srgb, var(--outline) 62%, transparent);\n  box-shadow: none;`, 'strategy deck utility hover');
  source = replaceCssRule(source, '.strategy-deck__toggle', `  background: color-mix(in srgb, #ffffff 92%, var(--lavender) 8%);`, 'strategy view-all button');
  source = replaceCssRule(source, '.strategy-deck', `  width: min(520px, 100%);\n  max-width: 100%;\n  margin-inline: auto;\n  margin-bottom: 0.75rem;\n  display: grid;\n  gap: 0.58rem;\n  touch-action: pan-y;`, 'strategy deck');

  source = replaceCssRule(source, '.strategy-form__container', `  display: grid;\n  gap: 0.45rem;`, 'strategy form container');
  source = replaceCssRule(source, '.strategy-card--form', `  display: grid;\n  padding: clamp(0.58rem, 1.6vw, 0.78rem);\n  gap: 0.42rem;\n  border: 1.5px solid color-mix(in srgb, var(--outline) 28%, transparent);\n  border-radius: var(--radius-xl);\n  background: color-mix(in srgb, var(--mint) 30%, #ffffff 70%);\n  box-shadow: none;`, 'personal strategy outer form');
  source = replaceCssRule(source, '.strategy-form', `  display: grid;\n  gap: 0.42rem;`, 'strategy form');
  source = replaceCssRule(source, '.strategy-form__field', `  display: grid;\n  gap: 0.16rem;\n  min-width: 0;`, 'strategy form field');
  source = replaceCssRule(source, '.strategy-form__field label', `  font: inherit;\n  font-size: 0.78rem;\n  font-weight: 720;\n  line-height: 1.2;\n  text-transform: none;\n  letter-spacing: 0;\n  color: var(--ink-soft);`, 'strategy form label');
  source = replaceCssRule(source, '.strategy-form__row', `  display: grid;\n  grid-template-columns: repeat(2, minmax(0, 1fr));\n  gap: 0.42rem;`, 'strategy form paired fields');
  source = replaceCssRule(source, '.strategy-card--input', `  display: block;\n  padding: 0;\n  overflow: hidden;`, 'strategy input wrapper');
  source = replaceCssRule(source, '.strategy-card--form .strategy-card--input', `  border: 1px solid color-mix(in srgb, var(--outline) 22%, transparent);\n  border-radius: var(--radius-md);\n  background: color-mix(in srgb, #ffffff 94%, var(--mint) 6%);\n  box-shadow: none;`, 'strategy form input surface');
  source = replaceCssRule(source, ".strategy-card--form .strategy-card--input input[type='text'],\n.strategy-card--form .strategy-card--input select,\n.strategy-card--form .strategy-card--input textarea", `  width: 100%;\n  min-height: 44px;\n  padding: 0.42rem 0.58rem;\n  border: 0;\n  background: transparent;\n  font: inherit;\n  font-size: 0.9rem;\n  color: inherit;\n  box-shadow: none;`, 'strategy form controls');
  source = replaceCssRule(source, '.strategy-card--form .strategy-card--input textarea', `  min-height: 4.75rem;\n  resize: vertical;`, 'strategy description textarea');
  source = replaceCssRule(source, '.strategy-card--input:focus-within', `  outline: 2px solid color-mix(in srgb, var(--plum) 48%, #ffffff 52%);\n  outline-offset: 1px;`, 'strategy input focus');
  source = replaceCssRule(source, '.strategy-card--form .strategy-card__actions', `  margin-top: 0.05rem;`, 'strategy action spacing');
  source = replaceCssRule(source, '.strategy-card__actions--save-targets .local-storage-note', `  grid-column: 1 / -1;\n  justify-self: stretch;\n  margin: 0.02rem 0 0;\n  text-align: left;\n  font-size: 0.68rem;\n  line-height: 1.3;\n  color: color-mix(in srgb, var(--ink-soft) 72%, transparent);`, 'save target helper note');

  write(path, source);
}

// 4. Permanent Bedrock regression: protect history-first Journal and final strategy hierarchy.
{
  const testPath = 'tests/final-hierarchy-ux.test.mjs';
  write(testPath, `import test from 'node:test';\nimport assert from 'node:assert/strict';\nimport { promises as fs } from 'node:fs';\nimport path from 'node:path';\nimport { fileURLToPath } from 'node:url';\nconst root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');\nconst load = (relative) => fs.readFile(path.join(root, relative), 'utf8');\n\ntest('Journal history is the primary surface and utilities are native disclosures', async () => {\n  const build = await load('scripts/build-pages.mjs');\n  const runtime = await load('scripts/inventory.js');\n  const html = await load('inventory/journal/index.html');\n  for (const source of [build, html]) {\n    assert.ok(source.includes('journal-fullscreen-button--compact'));\n    assert.ok(source.includes('>New entry<'));\n    assert.ok(source.includes('class="journal-history-controls__filters"'));\n    assert.ok(source.includes('journal-utility-disclosure'));\n    assert.ok(source.indexOf('journal-history-section journal-panel') < source.indexOf('journal-overview-grid'));\n    assert.equal(source.includes('data-journal-summary-toggle'), false);\n    assert.equal(source.includes('journal-page-description'), false);\n    assert.equal((source.match(/data-journal-history/g) || []).length, 1);\n  }\n  assert.equal(runtime.includes('journalSummaryToggle'), false);\n  assert.equal(runtime.includes('updateJournalSummaryVisibility'), false);\n});\n\ntest('Need strategy browsing and personal strategy editing use compact final hierarchy', async () => {\n  const build = await load('scripts/build-pages.mjs');\n  const css = await load('styles.css');\n  const need = await load('needs/acceptance/index.html');\n  assert.ok(build.includes('class="strategy-section__header"'));\n  assert.ok(need.includes('class="strategy-section__header"'));\n  assert.equal(need.includes('Shuffle cards'), false);\n  assert.ok(need.includes('>Shuffle</button>'));\n  assert.ok(css.includes('.strategy-section__header {'));\n  assert.ok(css.includes('background: color-mix(in srgb, var(--mint) 30%, #ffffff 70%);'));\n  assert.ok(css.includes('min-height: 4.75rem;'));\n  assert.ok(css.includes('font-size: 0.68rem;'));\n});\n`);

  const packagePath = 'package.json';
  const pkg = JSON.parse(read(packagePath));
  if (!pkg.scripts['test:flicker-jitter'].includes('tests/final-hierarchy-ux.test.mjs')) {
    pkg.scripts['test:flicker-jitter'] = `${pkg.scripts['test:flicker-jitter']} tests/final-hierarchy-ux.test.mjs`;
  }
  write(packagePath, `${JSON.stringify(pkg, null, 2)}\n`);

  const densityPath = 'tests/shared-density-polish.test.mjs';
  let density = read(densityPath);
  density = density.replace(
    "  assert.ok(buildPages.includes('class=\"journal-history-controls\" data-journal-filters'));",
    "  assert.ok(buildPages.includes('class=\"journal-history-controls\" data-journal-filters'));\n  assert.ok(buildPages.includes('class=\"journal-history-controls__filters\"'));\n  assert.ok(buildPages.includes('journal-utility-disclosure'));",
  );
  density = density.replace(
    "  assert.ok(journalHtml.includes('class=\"journal-history-controls\" data-journal-filters'));",
    "  assert.ok(journalHtml.includes('class=\"journal-history-controls\" data-journal-filters'));\n  assert.ok(journalHtml.includes('class=\"journal-history-controls__filters\"'));\n  assert.ok(journalHtml.includes('journal-utility-disclosure'));",
  );
  write(densityPath, density);
}

console.log('Final Bedrock hierarchy refactor applied.');
